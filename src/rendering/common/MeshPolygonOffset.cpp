#include "rendering/common/MeshPolygonOffset.h"

namespace PolygonOffset
{

const std::pair< float, float> imageSlices{ 1.0f, 1.0f }; // bottom
const std::pair< float, float> slideSlices{ -1.5f, -2.0f };
const std::pair< float, float> annotations{ -2.0f, -4.0f };
const std::pair< float, float> landmarks{ -2.5f, -5.0f };
const std::pair< float, float> crosshairs{ -3.0f, -6.0f }; // top

} // namespace PolygonOffset
